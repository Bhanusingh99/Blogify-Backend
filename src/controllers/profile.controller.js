import { User } from "../models/user.model";
import { BlogModel } from "../models/blog.model";

const updateProdile = async(req,res) => {
    try {
        const {firstname,lastname,bio,profilePicture} = req.body;
        
    } catch (error) {
        
    }
}